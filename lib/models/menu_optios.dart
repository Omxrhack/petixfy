import 'package:flutter/material.dart';

class MenuOptions {
  final String route;
  final String title;
  final Widget screen;

  MenuOptions({
    required this.route,
    required this.title,
    required this.screen,
  });
}
